# Comma-separated list of allowed web origins (in addition to any chrome-extension:// origins)
ALLOWED_ORIGINS=https://api.kity.app

# Explicit chrome-extension origin to allow (optional; chrome-extension://* allowed by default)
EXTENSION_ORIGIN=chrome-extension://<your-extension-id>

# Server port
PORT=8787
